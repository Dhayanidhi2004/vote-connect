fastapi>=0.115
uvicorn[standard]>=0.34
sqlalchemy>=2.0.36
pydantic>=2.11
pydantic-settings>=2.7
python-jose[cryptography]>=3.3
python-multipart>=0.0.20
email-validator>=2.2
# Postgres/Supabase driver — pure-Python (pg8000) so it installs without a
# compiler on any Python version. SQLite is used when DATABASE_URL is unset.
pg8000>=1.31
google-genai>=0.3
truststore>=0.10
